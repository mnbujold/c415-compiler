{
    9.pal
    Tests commas, reserved words, and unexpected symbols.
    Errors:
    11:14 unexpected comma, expecting '='
    16:8 unexpected 'end'
    24:12 unexpected greater than
}
program mostness(input_type, output_type);

type xType = (x1, x2, x3);

var xArray = array[xType] of integer;
    y : xType;
    input_type : char; // Illegal - redefining input_type

function canIReturn(x : xType; i : integer) : xType;
    var output_type : char; // Legal.
    
begin
    if (xArray[x] = i) then
        y := x // Error - could not find return value for all conditions
    else
        canIReturn := canIReturn(x, i + 1)
end;

function doIReturn(var y : boolean) : integer;
begin
    if y then
        doIReturn := 5
    else
        y := true
end; // Error - could not find return value for all conditionals

function returnValue() : integer;
begin
    while false do
        returnValue := 0;

    if doIReturn(y = x1) then
        returnValue := 5
end; // Error - could not find return value for all conditionals

function correctReturn(int : integer) : boolean;
begin
    if int >= 19 then
        correctReturn := true
    else
        if int <= -10 then
            correctReturn := false
        else if int <= 0 then
            correctReturn := true
        else
            correctReturn := false
end;

begin

mostness(input_type, output_type); // Illegal!

end.
