{
    5.pal
    Tests reserved words and comment syntactical errors.
    Expected output:
    11:1 unexpected string
    14:6 expecting identifier but found 'begin' (reserved word)
    14:21 expecting expression but found 'if' (reserved word)
}
program syntactical(input, output);
// Does some stuff with words and sentences.
/' This comment should break! { not a comment }
{ syntax error above}

type begin = array[aFunction(true)..5] of integer; { syntax error here }

var bob : str;

function aFunction(question : boolean) : integer;
begin
    aFunction := 3;
    if bob then bob else bob
    if (not bob or false) then begin french := boolean; bob17.gbg and why(); end;
    else if if 'lol4567' then thisShouldBreak begin { syntax error here }
        anotherThing := 234;
    end;
    
    writeln(question);
end;

begin

bob[3] := 3;

end.
