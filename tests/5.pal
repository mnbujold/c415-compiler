{
    5.pal
    Tests reserved words and comment syntactical errors.
    Expected output:
    #:6 expecting identifier but found 'begin' (reserved word)
    #:21 expecting expression but found 'if' (reserved word)
    #:1 unexpected divide
    
}
program syntactical(input, output);

type begin = array[aFunction(true)..5] of integer; { syntax error here }

var bob : str;

function aFunction(question : boolean) : integer;
begin
    aFunction := 3;
    if bob then bob() else bob := 4
    if (not bob or false) then begin french := boolean; ool:=bob17.gbg and why(); end;
    else if if 'lol4567' then thisShouldBreak begin { syntax error here }
        anotherThing := 234;
    end;
    
    writeln(question);
end;

/' This comment should break! { not a comment }
{ syntax error above}

begin

bob[3] := 3;

end.
