{
Tests if the compiler allocates the space for vars correctly
}
program correct (input, output);

type tf = (true, false);

var 
    numOdd: real;
    numEven: real;
    numReal: integer;
    chararray: array [1..10] of char;
    bool: tf;
    
    begin
        writeln;
    end.
    