// Get the type checking to work ...

program testTypes(input, output);

const aNum = 1;
      aNum = 2;
      anotherbob = aNum;

type bob = (enum1, enum2);
     clark = bob;
     enum0 = (e00, e01, e02);
     bool = boolean;
     foo = array[1..10] of char;

var joe : bob;
    text : clark;
    bar : foo;
    e11 : enum0;

procedure foo(joe : enum0; foo :enum0);
    type enum1 = (e00, e11, e02);
         joe = bool;
    begin
    end;
    
    
function asdf(tool : bob) : bob;
begin
    asdf := enum2;
end;
    
    
procedure tim();
    var joe : (e1, ererer);
begin
    //joe := not 2;
end;
    
begin

end.
