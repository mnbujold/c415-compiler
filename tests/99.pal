// Get the type checking to work ...

program testTypes(input, output);

const aNum = 1;
      aNum = 2;
      anotherbob = aNum;

type bob = (enum1, enum2);
     str = 'a string'; // Should be a syntactic error!
     clark = bob;
     enum0 = (e00, e01, e02);
     bool = boolean;
     foo = array[1..10] of 54;

var joe : bob;
    text : clark;
    bar : foo;

procedure foo();
    type enum1 = (e00, e11, e02);
    begin
    end;
    
procedure tim();
    var joe : (e1, ererer);
begin
    joe := not 2;
end;
    
begin

end.
