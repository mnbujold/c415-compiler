{
Tests expressions
}
program correct (input, output);

type tf = (true, false);
     book = record
                title : array[1..50] of char;
                author : array[1..25] of char;
                id : integer
            end;

var 
    numOdd: real;
    numEven: real;
    numReal: integer;

    procedure joe ();
        begin
          if (1 < 2) then
            numReal := 1 + 3;
        end;
    
    
    begin
        joe ();
        numReal := 1 + 2;
        numReal := 1 + 2 * 3;
        numReal := (1 + 2) * 3;
        joe();
        //writeln;
        //writeln (sin (0));
        //writeln;
    end.
    