{
    7.pal
    Tests missing identifiers and bad real numbers.
    Errors:
    9:23 unexpected paranthesis, missing identifier
    13:16 incorrect real number (unexpecting period)
    14:13 incorrect real number (unexpecting period)
}
program aTHINGY(fsdf, bobbingForApples);



type badEnum = (enum0, enum0, enum1); // Illegal - redefining an enum!
     a = char;  // Legal.
     char = real; // Legal.
     char = char; // Illegal!

begin

end.
