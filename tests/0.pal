{
    0.pal
    Tests invalid characters.
    Errors:
    25:23 illegal character '!'
    43:20 illegal character '#'
    73:4 bad end.
}
program lexical(input, output);
// Does some stuff with words and sentences. {}}}}}}}}}}}}}}

const BOB = 'bob';
      SENTENCELENGTH = 3;
      WORDLENGTH = 10;
      GOOD = true;
      BAD = false;
      FORCECONST = 10;! { lexical error here }

type str = array[1..10] of char;
     terms = (lexical, semantic, syntactical);

var sent1, sent2, sent3 : array[1..3] of bolean;
    aterm : terms;
    worked : boolean;

function setWord(in#dex : integer; newWord : str) { lexical error here }
                : boolean;
begin
    aTerm := lexical;
    if index <= SENTENCELENGTH then begin
        sent[index] := newWord;
        setWord := GOOD; end;
end;
begin

sent1 := sentence();
worked := setWord(1, 'bob');
writeln(worked);
writeln(FORCECONST);

end. { syntax error here }
