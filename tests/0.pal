{
    0.pal
    Tests comment and real constant lexical errors.
    Errors:
    17:30 illegal character '!'
    19:9 illegal character '|'
    26:21 illegal character '#'
}
program lexical(input, output);
// Does some stuff with words and sentences. {}}}}}}}}}}}}}}

const BOB = 'bob';
      SENTENCELENGTH = 3;
      WORDLENGTH = 10;
      GOOD = true;
      BAD = false;
      FORCECONST = 10;! { lexical error here }

type str| = array[1..10] of char;   { lexical error here }
     terms = (lexical, semantic, syntactical);

var sent1, sent2, sent3 : array[1..3] of bolean;
    aterm : terms;
    worked : boolean;

function setWord(in#dex : integer; newWord : str) { lexical error here }
                : boolean;
begin
    aTerm := lexical;
    if index <= SENTENCELENGTH then begin
        sent[index] := newWord;
        setWord := GOOD; 
    else
        setWord := BAD
        end;
end;
begin

sent1 := sentence();
worked := setWord(1, 'bob');
writeln(worked);
writeln(FORCECONST);

end.
